const theme = {
  fontFamily: 'Inter',
  colors: {
    primary: '#FEF200',
    success: '#A1FB56',
    danger: '#E30000',
    backgroundMain: '#020202',
  },
};

export default theme;

export type ThemeType = typeof theme;
