
stages:
    - test
    - deploy

#image: ubuntu
#before_script:
#    - apt-get update -qq
#    - apt-get install -qq git curl
#    - 'which ssh-agent || ( apt-get install -qq openssh-client )'
#    - eval $(ssh-agent -s)
#    - ssh-add <(echo "$SSH_PRIVATE_KEY")
#    - mkdir -p ~/.ssh
#    - chmod 700 ~/.ssh
#    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
#    - git config --global user.email "olef_and@ukr.net"
#    - git config --global user.name "andrii"
  
#test_package:
#    stage: test
#    script:
#      - >-
#        echo "nice"

update_master:
    stage: deploy
    script:
      - >-
        echo $PATH
        && ls -lahs $HOME/
        && pwd
        && cd $HOME/dom-pwa/
        && sudo GIT_SSH_COMMAND='ssh -i /root/.ssh/gitlab_rsa' git pull
        && whoami
        && pwd
        && yarn
        && yarn build
        && exit
      
    only:
      - main